import { AppError } from '../../shared/errors.js';

export function runRuleSequence() {
  throw new AppError(500, 'ENGINE_NOT_IMPLEMENTED', 'Rule executor lands in TASK-013 (05-PAYROLL-ENGINE-CONTRACT.md)');
}
