import { AppError } from '../../shared/errors.js';

export function evaluateFormula() {
  throw new AppError(500, 'ENGINE_NOT_IMPLEMENTED', 'Safe formula evaluator lands in TASK-013 (05-PAYROLL-ENGINE-CONTRACT.md)');
}
